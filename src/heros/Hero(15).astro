---
//heros/Hero(15).astro
const { whatsappLink1, whatsappLink2, pixelEvent } = Astro.props;
import '../styles/global.css';
---

<section class="relative min-h-screen pt-32 sm:pt-36 md:pt-40 flex flex-col items-center bg-gradient-to-b from-casino-purple via-[#241554] to-[#1A0F36] overflow-hidden w-full">
  <!-- Efectos de luz fluida en el fondo -->
  <div class="fluid-light fluid-red"></div>
  <div class="fluid-light fluid-blue"></div>
  <div class="fluid-light fluid-red-small"></div>
  <div class="fluid-light fluid-blue-small"></div>

  <!-- Elementos flotantes decorativos -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="floating-element absolute top-1/4 left-10 w-12 h-12 opacity-30">
      <img src="/images/chips.svg" alt="" class="w-full h-full" />
    </div>
    <div class="floating-element absolute top-2/3 right-12 w-16 h-16 opacity-20" style="animation-delay: 1s;">
      <img src="/images/ace.svg" alt="" class="w-full h-full" />
    </div>
    <div class="floating-element absolute bottom-1/4 left-1/3 w-14 h-14 opacity-25" style="animation-delay: 0.5s;">
      <img src="/images/gambling.svg" alt="" class="w-full h-full" />
    </div>
    <div class="floating-element absolute top-1/3 right-1/4 w-10 h-10 opacity-20" style="animation-delay: 1.5s;">
      <img src="/images/ace.svg" alt="" class="w-full h-full" />
    </div>
  </div>
  
  <!-- Efecto de luz de fondo -->
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full h-full max-w-lg max-h-lg bg-casino-gold/10 blur-[100px] rounded-full"></div>
  
  <!-- Textura de fondo unificada -->
  <div class="absolute inset-0 bg-felt-pattern opacity-10"></div>
  <div class="absolute inset-0 bg-dots-pattern opacity-5"></div>
  
  <div class="w-full px-5 max-w-4xl mx-auto flex flex-col items-center justify-center relative z-10 text-center">
    <!-- TITLE -->
    <h1 class="text-3xl sm:text-4xl md:text-5xl mb-10 text-white uppercase hero-title font-extrabold tracking-wide">
      APROVECHA Y <span class="gradient-text">GANA TU BONO!</span>
    </h1>
    
    <!-- Botón de WhatsApp visible arriba -->
    <div class="mb-8 w-full max-w-sm mx-auto">
      <a id="whatsapp-top-button" href={whatsappLink1} target="_blank" rel="noopener" class="flex items-center justify-center gap-2 bg-green-600 hover:bg-green-700 text-white font-bold py-4 px-6 rounded-xl transition-all hover:scale-105 shadow-lg w-full text-xl">
        <svg width="28" height="28" viewBox="0 0 308 308" fill="white" xmlns="http://www.w3.org/2000/svg">
          <path d="M227.904,176.981c-0.6-0.288-23.054-11.345-27.044-12.781c-1.629-0.585-3.374-1.156-5.23-1.156c-3.032,0-5.579,1.511-7.563,4.479c-2.243,3.334-9.033,11.271-11.131,13.642c-0.274,0.313-0.648,0.687-0.872,0.687c-0.201,0-3.676-1.431-4.728-1.888c-24.087-10.463-42.37-35.624-44.877-39.867c-0.358-0.61-0.373-0.887-0.376-0.887c0.088-0.323,0.898-1.135,1.316-1.554c1.223-1.21,2.548-2.805,3.83-4.348c0.607-0.731,1.215-1.463,1.812-2.153c1.86-2.164,2.688-3.844,3.648-5.79l0.503-1.011c2.344-4.657,0.342-8.587-0.305-9.856c-0.531-1.062-10.012-23.944-11.02-26.348c-2.424-5.801-5.627-8.502-10.078-8.502c-0.413,0,0,0-1.732,0.073c-2.109,0.089-13.594,1.601-18.672,4.802c-5.385,3.395-14.495,14.217-14.495,33.249c0,17.129,10.87,33.302,15.537,39.453c0.116,0.155,0.329,0.47,0.638,0.922c17.873,26.102,40.154,45.446,62.741,54.469c21.745,8.686,32.042,9.69,37.896,9.69c0.001,0,0.001,0,0.001,0c2.46,0,4.429-0.193,6.166-0.364l1.102-0.105c7.512-0.666,24.02-9.22,27.775-19.655c2.958-8.219,3.738-17.199,1.77-20.458C233.168,179.508,230.845,178.393,227.904,176.981z" />
          <path d="M156.734,0C73.318,0,5.454,67.354,5.454,150.143c0,26.777,7.166,52.988,20.741,75.928L0.212,302.716c-0.484,1.429-0.124,3.009,0.933,4.085C1.908,307.58,2.943,308,4,308c0.405,0,0.813-0.061,1.211-0.188l79.92-25.396c21.87,11.685,46.588,17.853,71.604,17.853C240.143,300.27,308,232.923,308,150.143C308,67.354,240.143,0,156.734,0z M156.734,268.994c-23.539,0-46.338-6.797-65.936-19.657c-0.659-0.433-1.424-0.655-2.194-0.655c-0.407,0-0.815,0.062-1.212,0.188l-40.035,12.726l12.924-38.129c0.418-1.234,0.209-2.595-0.561-3.647c-14.924-20.392-22.813-44.485-22.813-69.677c0-65.543,53.754-118.867,119.826-118.867c66.064,0,119.812,53.324,119.812,118.867C276.546,215.678,222.799,268.994,156.734,268.994z" />
        </svg>
        CREAR USUARIO AHORA
      </a>
    </div>
    
    <!-- Contenedor del juego de vasos -->
    <div class="game-container relative w-full max-w-lg mx-auto flex flex-col items-center justify-center bg-purple-900/50 backdrop-blur-sm p-5 rounded-xl">
      <!-- Juego de los vasos -->
      <div class="cups-game-container w-full">
        <h3 class="text-white text-xl mb-4 font-bold">Encuentra la pelota para ganar tu BONO</h3>
        
        <div id="game-instruction" class="text-yellow-300 text-lg mb-5">¡Selecciona un vaso!</div>
        
        <!-- Contenedor de los vasos con posiciones fijas -->
        <div id="cups-container" class="flex justify-between items-end mb-8 relative h-[180px] w-full">
          <div class="cup-position" id="position-0">
            <div class="cup" data-index="0">
              <div class="ball"></div>
            </div>
          </div>
          <div class="cup-position" id="position-1">
            <div class="cup" data-index="1">
              <div class="ball"></div>
            </div>
          </div>
          <div class="cup-position" id="position-2">
            <div class="cup" data-index="2">
              <div class="ball"></div>
            </div>
          </div>
        </div>
        
        <button id="shuffle-cups-btn" class="bg-gradient-to-r from-yellow-500 to-amber-500 text-black hover:from-amber-500 hover:to-yellow-500 text-xl font-bold py-3 px-6 rounded-xl transition-all hover:scale-105 shadow-2xl flex items-center justify-center gap-3 mx-auto">
          <span class="shuffle-icon">🔄</span>
          MEZCLAR VASOS
        </button>
      </div>
    </div>
  </div>
  
  <!-- Modal de bono a pantalla completa -->
  <div id="bonus-modal" class="fixed inset-0 flex items-center justify-center z-50 opacity-0 pointer-events-none transition-opacity duration-500">
    <div class="modal-backdrop absolute inset-0 bg-black/80 backdrop-blur-md"></div>
    <div class="modal-card-container relative w-full h-full flex items-center justify-center">
      <!-- Carta central animada -->
      <div class="modal-card transform transition-all duration-700 scale-0">
        <div class="modal-card-inner relative bg-gradient-to-b from-[#281552] to-[#1A0F36] rounded-xl border-4 border-casino-gold/60 shadow-2xl overflow-hidden w-full max-w-lg mx-auto">
          <!-- Cabecera del modal -->
          <div class="modal-header bg-gradient-to-r from-[#281552] to-[#1E1040] p-6 text-center border-b border-casino-gold/30 relative">
            <button id="close-modal" class="absolute right-4 top-4 text-white hover:text-casino-gold transition-colors duration-200">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
            <h2 id="modal-title" class="text-3xl font-bold text-white">¡<span class="bonus-name">BONO</span>!</h2>
          </div>
          
          <!-- Contenido del modal -->
          <div class="modal-content p-6">
            <div class="bonus-icon-container flex justify-center mb-6">
              <div class="bonus-icon w-24 h-24 rounded-full bg-gradient-to-br from-casino-gold/30 to-casino-gold/10 flex items-center justify-center">
                <div id="modal-icon" class="text-5xl">🎁</div>
              </div>
            </div>
            
            <div class="bonus-details text-center mb-8">
              <h3 id="modal-subtitle" class="text-xl text-white font-bold mb-4">¡Felicidades has ganado tu bono!</h3>
              <p id="modal-description" class="text-white/90 text-lg mb-6">Hablanos al whatsapp pulsando el boton y crea tu usuario al instante</p>
              
              <div class="bonus-features space-y-4 text-left mb-8">
                <div class="feature flex items-center">
                  <div class="feature-icon w-10 h-10 bg-casino-gold/20 rounded-full flex items-center justify-center text-white mr-4">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                  </div>
                  <div id="feature-1" class="feature-text text-white/90">Característica 1</div>
                </div>
                
                <div class="feature flex items-center">
                  <div class="feature-icon w-10 h-10 bg-casino-gold/20 rounded-full flex items-center justify-center text-white mr-4">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                  </div>
                  <div id="feature-2" class="feature-text text-white/90">Característica 2</div>
                </div>
                
                <div class="feature flex items-center">
                  <div class="feature-icon w-10 h-10 bg-casino-gold/20 rounded-full flex items-center justify-center text-white mr-4">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                  </div>
                  <div id="feature-3" class="feature-text text-white/90">Característica 3</div>
                </div>
              </div>
            </div>
            
            <!-- Botones de acción -->
            <div class="modal-actions flex flex-col space-y-4">
              <a id="modal-cta" href={whatsappLink1} target="_blank" rel="noopener" class="cta-button w-full flex items-center justify-center gap-3 bg-gradient-to-r from-green-600 to-green-500 hover:from-green-500 hover:to-green-600 text-white text-xl font-bold py-4 px-6 rounded-xl transition-all hover:scale-105 shadow-xl">
                <svg width="24" height="24" viewBox="0 0 308 308" fill="white" xmlns="http://www.w3.org/2000/svg">
                  <path d="M227.904,176.981c-0.6-0.288-23.054-11.345-27.044-12.781c-1.629-0.585-3.374-1.156-5.23-1.156c-3.032,0-5.579,1.511-7.563,4.479c-2.243,3.334-9.033,11.271-11.131,13.642c-0.274,0.313-0.648,0.687-0.872,0.687c-0.201,0-3.676-1.431-4.728-1.888c-24.087-10.463-42.37-35.624-44.877-39.867c-0.358-0.61-0.373-0.887-0.376-0.887c0.088-0.323,0.898-1.135,1.316-1.554c1.223-1.21,2.548-2.805,3.83-4.348c0.607-0.731,1.215-1.463,1.812-2.153c1.86-2.164,2.688-3.844,3.648-5.79l0.503-1.011c2.344-4.657,0.342-8.587-0.305-9.856c-0.531-1.062-10.012-23.944-11.02-26.348c-2.424-5.801-5.627-8.502-10.078-8.502c-0.413,0,0,0-1.732,0.073c-2.109,0.089-13.594,1.601-18.672,4.802c-5.385,3.395-14.495,14.217-14.495,33.249c0,17.129,10.87,33.302,15.537,39.453c0.116,0.155,0.329,0.47,0.638,0.922c17.873,26.102,40.154,45.446,62.741,54.469c21.745,8.686,32.042,9.69,37.896,9.69c0.001,0,0.001,0,0.001,0c2.46,0,4.429-0.193,6.166-0.364l1.102-0.105c7.512-0.666,24.02-9.22,27.775-19.655c2.958-8.219,3.738-17.199,1.77-20.458C233.168,179.508,230.845,178.393,227.904,176.981z" />
                  <path d="M156.734,0C73.318,0,5.454,67.354,5.454,150.143c0,26.777,7.166,52.988,20.741,75.928L0.212,302.716c-0.484,1.429-0.124,3.009,0.933,4.085C1.908,307.58,2.943,308,4,308c0.405,0,0.813-0.061,1.211-0.188l79.92-25.396c21.87,11.685,46.588,17.853,71.604,17.853C240.143,300.27,308,232.923,308,150.143C308,67.354,240.143,0,156.734,0z M156.734,268.994c-23.539,0-46.338-6.797-65.936-19.657c-0.659-0.433-1.424-0.655-2.194-0.655c-0.407,0-0.815,0.062-1.212,0.188l-40.035,12.726l12.924-38.129c0.418-1.234,0.209-2.595-0.561-3.647c-14.924-20.392-22.813-44.485-22.813-69.677c0-65.543,53.754-118.867,119.826-118.867c66.064,0,119.812,53.324,119.812,118.867C276.546,215.678,222.799,268.994,156.734,268.994z" />
                </svg>
                CREAR USUARIO
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script define:vars={{ pixelEvent }}>
  document.addEventListener('DOMContentLoaded', () => {
    // Referencias a elementos del DOM
    const bonusModal = document.getElementById('bonus-modal');
    const modalCard = document.querySelector('.modal-card');
    const closeModalBtn = document.getElementById('close-modal');
    const mainWhatsappLink = document.querySelector('#modal-cta')?.getAttribute('href') || '';
    
    // Para depuración
    console.log('Pixel event disponible:', pixelEvent);
    
    // Configurar el botón de WhatsApp superior
    const topWhatsappButton = document.getElementById('whatsapp-top-button');
    if (topWhatsappButton) {
      topWhatsappButton.addEventListener('click', (e) => {
        // Prevenir navegación inmediata
        e.preventDefault();
        
        // Usar una variable local para el evento
        const eventToTrack = pixelEvent || 'Lead';
        
        // Imprimir para depuración
        console.log('Tracking event desde botón superior:', eventToTrack);
        
        // Enviar el evento si fbq está disponible
        if (typeof fbq === 'function') {
          fbq('track', eventToTrack);
        }
        
        // Navegar después de un pequeño retraso
        setTimeout(() => {
          window.open(topWhatsappButton.href, '_blank');
        }, 300);
      });
    }
    
    // Obtener el botón CTA
    const ctaButton = document.getElementById('modal-cta');
    
    if (ctaButton) {
      ctaButton.addEventListener('click', (e) => {
        // Prevenir navegación inmediata
        e.preventDefault();
        
        // Usar una variable local para el evento
        const eventToTrack = pixelEvent || 'Lead';
        
        // Imprimir para depuración
        console.log('Tracking event:', eventToTrack);
        
        // Enviar el evento si fbq está disponible
        if (typeof fbq === 'function') {
          fbq('track', eventToTrack);
        }
        
        // Navegar después de un pequeño retraso
        setTimeout(() => {
          window.location.href = ctaButton.href;
        }, 300);
      });
    }
    
    // Datos de los bonos
    const bonusData = {
      'default': {
        name: '10% BONO',
        icon: '🎁',
        subtitle: '¡Felicidades has ganado tu bono!',
        description: 'Hablanos al whatsapp pulsando el boton y crea tu usuario al instante',
        features: [
          'Válido para cualquier monto de carga',
          'Sin requisitos adicionales',
          'Actívalo inmediatamente al crear tu usuario'
        ],
        ctaLink: ''
      }
    };
    
    // Función para mostrar el modal con los detalles del bono
    function showBonusModal() {
      const bonus = bonusData['default'];
      
      // Actualizar contenido del modal
      const bonusNameElement = document.querySelector('.bonus-name');
      if (bonusNameElement) bonusNameElement.textContent = bonus.name;
      
      const modalIcon = document.getElementById('modal-icon');
      const modalSubtitle = document.getElementById('modal-subtitle');
      const modalDescription = document.getElementById('modal-description');
      const feature1 = document.getElementById('feature-1');
      const feature2 = document.getElementById('feature-2');
      const feature3 = document.getElementById('feature-3');
      
      if (modalIcon) modalIcon.textContent = bonus.icon;
      if (modalSubtitle) modalSubtitle.textContent = bonus.subtitle;
      if (modalDescription) modalDescription.textContent = bonus.description;
      if (feature1) feature1.textContent = bonus.features[0];
      if (feature2) feature2.textContent = bonus.features[1];
      if (feature3) feature3.textContent = bonus.features[2];
      
      // Actualizar link de CTA
      const ctaButton = document.getElementById('modal-cta');
      if (ctaButton) {
        ctaButton.href = mainWhatsappLink;
      }
      
      // Mostrar el modal con animación
      if (bonusModal) {
        bonusModal.classList.remove('opacity-0', 'pointer-events-none');
        setTimeout(() => {
          if (modalCard) modalCard.classList.add('scale-100');
        }, 100);
      }
    }
    
    // Cerrar el modal
    function closeModal() {
      if (modalCard) modalCard.classList.remove('scale-100');
      
      setTimeout(() => {
        if (bonusModal) bonusModal.classList.add('opacity-0', 'pointer-events-none');
      }, 300);
    }
    
    // Evento para cerrar el modal
    if (closeModalBtn) {
      closeModalBtn.addEventListener('click', closeModal);
    }
    
    //////////////////////////////////////////
    // JUEGO DE VASOS
    //////////////////////////////////////////
    
    // Variables del juego
    let gameAttempts = 0;
    let winningCupIndex = 0;
    let isGameActive = false;
    let isShuffling = false;
    
    // Mapeo de qué vaso está en cada posición
    // Al inicio, el vaso 0 está en posición 0, vaso 1 en posición 1, etc.
    const cupPositions = {
      0: 0, // El vaso con índice 0 está en la posición 0
      1: 1, // El vaso con índice 1 está en la posición 1
      2: 2  // El vaso con índice 2 está en la posición 2
    };
    
    // Referencias a elementos del DOM para el juego
    const cupsContainer = document.getElementById('cups-container');
    const cups = document.querySelectorAll('.cup');
    const cupPositionsElements = [
      document.getElementById('position-0'),
      document.getElementById('position-1'),
      document.getElementById('position-2')
    ];
    const balls = document.querySelectorAll('.ball');
    const shuffleButton = document.getElementById('shuffle-cups-btn');
    const gameInstruction = document.getElementById('game-instruction');
    
    // Función para mostrar brevemente dónde está la pelota con animación
    function showBallBriefly(cupIndex) {
      console.log(`Mostrando pelota bajo el vaso ${cupIndex + 1}`);
      
      const cup = document.querySelector(`.cup[data-index="${cupIndex}"]`);
      if (!cup) {
        console.error('Error: No se encontró el vaso para mostrar la pelota');
        return;
      }
      
      const ball = cup.querySelector('.ball');
      if (!ball) {
        console.error('Error: No se encontró la pelota dentro del vaso');
        return;
      }
      
      // 1. Elevar el vaso para mostrar la pelota con una animación más pronunciada
      cup.classList.add('reveal');
      cup.style.animation = 'highlight-cup 0.4s ease-out';
      
      // 2. Mostrar la pelota con un efecto de aparición
      setTimeout(() => {
        ball.style.display = 'block';
        ball.style.opacity = '0';
        ball.style.transform = 'scale(0.5)';
        
        // Animar la aparición de la pelota
        setTimeout(() => {
          ball.style.transition = 'opacity 0.3s ease, transform 0.3s ease';
          ball.style.opacity = '1';
          ball.style.transform = 'scale(1)';
        }, 50);
      }, 400);
      
      // 3. Bajar el vaso para cubrir la pelota
      setTimeout(() => {
        cup.classList.remove('reveal');
        cup.style.animation = '';
      }, 1500);
      
      // 4. Ocultar la pelota después de que el vaso esté abajo
      setTimeout(() => {
        ball.style.display = 'none';
        ball.style.opacity = '';
        ball.style.transform = '';
      }, 1800);
    }
    
    // Función para iniciar el juego
    function startGame() {
      if (isShuffling || isGameActive) return;
      
      console.log('Iniciando juego');
      
      // Reset cup positions visually
      cups.forEach(cup => {
        cup.style.transition = 'none';
        cup.style.transform = 'none';
        cup.classList.remove('lifted', 'winner', 'shake', 'reveal');
        cup.querySelector('.ball').style.display = 'none';
        
        // Move cups back to original positions
        const cupIndex = parseInt(cup.getAttribute('data-index'));
        const currentPosition = findCupPosition(cupIndex);
        const originalPosition = cupPositionsElements[cupIndex];
        
        if (currentPosition !== originalPosition) {
          // If not in original position, move it back
          if (currentPosition) {
            currentPosition.removeChild(cup);
          }
          if (originalPosition) {
            originalPosition.appendChild(cup);
          }
        }
      });
      
      // Reset positions map
      for (let i = 0; i < 3; i++) {
        cupPositions[i] = i;
      }
      
      // Incrementar intentos
      gameAttempts++;
      console.log(`Intento #${gameAttempts}`);
      
      // Determinar vaso ganador (en el tercer intento siempre gana)
      winningCupIndex = Math.floor(Math.random() * 3);
      console.log(`Vaso ganador: ${winningCupIndex + 1}`);
      
      // Actualizar instrucción
      if (gameInstruction) {
        gameInstruction.textContent = 'Observa bien dónde está la pelota';
      }
      
      // Reactivar transiciones después del reset
      setTimeout(() => {
        cups.forEach(cup => {
          cup.style.transition = 'transform 0.3s ease-out';
        });
        
        // Mostrar brevemente la pelota con animación mejorada
        showBallBriefly(winningCupIndex);
        
        // Ocultar después de un momento y comenzar a mezclar
        setTimeout(() => shuffleCups(), 2500); // Aumentar tiempo para que la animación sea más visible
      }, 50);
    }
    
    // Encuentra el elemento de posición que contiene un vaso específico
    function findCupPosition(cupIndex) {
      for (const position of cupPositionsElements) {
        const cup = position.querySelector(`.cup[data-index="${cupIndex}"]`);
        if (cup) return position;
      }
      return null;
    }
    
    // Intercambia físicamente dos vasos entre posiciones
    function swapCups(pos1, pos2) {
      console.log(`Intercambiando vasos en posiciones ${pos1} y ${pos2}`);
      
      const position1 = cupPositionsElements[pos1];
      const position2 = cupPositionsElements[pos2];
      
      const cup1 = position1.querySelector('.cup');
      const cup2 = position2.querySelector('.cup');
      
      if (!cup1 || !cup2) {
        console.error('Error: No se encontraron los vasos para intercambiar');
        return;
      }
      
      // Obtener los índices de los vasos
      const cup1Index = parseInt(cup1.getAttribute('data-index'));
      const cup2Index = parseInt(cup2.getAttribute('data-index'));
      
      // Animación inicial - elevar vasos
      cup1.style.transform = 'translateY(-20px)';
      cup2.style.transform = 'translateY(-20px)';
      
      // Después de elevarse, mover horizontalmente
      setTimeout(() => {
        cup1.style.transform = `translateY(-20px) translateX(${position2.offsetLeft - position1.offsetLeft}px)`;
        cup2.style.transform = `translateY(-20px) translateX(${position1.offsetLeft - position2.offsetLeft}px)`;
        
        // Después del movimiento horizontal, intercambiar los vasos en el DOM
        setTimeout(() => {
          // Eliminar transformaciones
          cup1.style.transform = 'none';
          cup2.style.transform = 'none';
          
          // Eliminar los vasos de sus posiciones actuales
          position1.removeChild(cup1);
          position2.removeChild(cup2);
          
          // Añadir los vasos a sus nuevas posiciones
          position1.appendChild(cup2);
          position2.appendChild(cup1);
          
          // Actualizar mapeo de posiciones
          cupPositions[cup1Index] = pos2;
          cupPositions[cup2Index] = pos1;
          
          // Actualizar el índice del vaso ganador si es necesario
          if (winningCupIndex === cup1Index) {
            winningCupIndex = cup2Index;
          } else if (winningCupIndex === cup2Index) {
            winningCupIndex = cup1Index;
          }
        }, 400); // Tiempo para el movimiento horizontal
      }, 200); // Tiempo para elevarse
    }
    
    // Animación para mezclar los vasos
    function shuffleCups() {
      isShuffling = true;
      
      // Actualizar instrucción
      if (gameInstruction) {
        gameInstruction.textContent = 'Mezclando...';
      }
      
      // Secuencia de mezcla
      let shuffleCount = 0;
      const maxShuffles = 6;
      
      // Configuración de la mezcla - pares predefinidos
      const swapPairs = [
        [0, 1], // Intercambiar vasos en posiciones 0 y 1
        [1, 2], // Intercambiar vasos en posiciones 1 y 2
        [0, 2], // Intercambiar vasos en posiciones 0 y 2
        [0, 1], // Repetir para más mezclas
        [1, 2],
        [0, 2]
      ];
      
      function performNextSwap() {
        if (shuffleCount >= maxShuffles) {
          finishShuffling();
          return;
        }
        
        const [pos1, pos2] = swapPairs[shuffleCount];
        swapCups(pos1, pos2);
        
        shuffleCount++;
        setTimeout(performNextSwap, 800); // 800ms entre cada intercambio
      }
      
      function finishShuffling() {
        isShuffling = false;
        isGameActive = true;
        
        // Actualizar instrucción
        if (gameInstruction) {
          gameInstruction.textContent = '¡Selecciona un vaso!';
        }
        
        // Identificar cuál es ahora el vaso ganador
        const winningCup = document.querySelector(`.cup[data-index="${winningCupIndex}"]`);
        console.log(`Mezcla finalizada. Vaso ganador es ${winningCupIndex + 1}`);
      }
      
      // Comenzar secuencia de mezcla
      setTimeout(performNextSwap, 300);
    }
    
    // Función para seleccionar un vaso
    function selectCup(cupElement) {
      if (!isGameActive || isShuffling) return;
      isGameActive = false;
      
      const cupIndex = parseInt(cupElement.getAttribute('data-index'));
      console.log(`Vaso seleccionado: ${cupIndex + 1}`);
      
      // Determinar si es ganador
      let isWinner = cupIndex === winningCupIndex;
      
      // Forzar victoria en el tercer intento
      if (gameAttempts === 3) {
        isWinner = true;
        winningCupIndex = cupIndex;
      }
      
      // Levantar el vaso seleccionado
      cupElement.classList.add('lifted');
      
      // Mostrar o no la pelota según corresponda
      if (isWinner) {
        const ball = cupElement.querySelector('.ball');
        if (ball) ball.style.display = 'block';
      } else {
        // En caso de perder, mostrar dónde estaba la pelota
        setTimeout(() => {
          const winnerCup = document.querySelector(`.cup[data-index="${winningCupIndex}"]`);
          if (winnerCup) {
            winnerCup.classList.add('lifted');
            const ball = winnerCup.querySelector('.ball');
            if (ball) ball.style.display = 'block';
          }
        }, 800);
      }
      
      // Actualizar instrucción
      if (gameInstruction) {
        gameInstruction.textContent = isWinner ? 
          '¡Felicidades! ¡Encontraste la pelota!' : 
          'No has acertado esta vez. Inténtalo de nuevo.';
      }
      
      // Manejar resultado
      setTimeout(() => {
        if (isWinner) {
          // Animación de victoria
          cupElement.classList.add('winner');
          
          // Mostrar modal después de un momento
          setTimeout(() => {
            showBonusModal();
            // Resetear contador de intentos
            gameAttempts = 0;
          }, 1000);
        } else {
          // Animar vaso perdedor
          cupElement.classList.add('shake');
          
          // Resetear juego después de un momento
          setTimeout(() => {
            cups.forEach(cup => {
              cup.classList.remove('lifted', 'shake', 'winner');
              cup.querySelector('.ball').style.display = 'none';
            });
            
            if (gameInstruction) {
              gameInstruction.textContent = 'Haz clic en MEZCLAR para volver a intentar';
            }
          }, 2500);
        }
      }, 1000);
    }
    
    // Agregar eventos a los vasos
    cups.forEach(cup => {
      cup.addEventListener('click', () => {
        if (isGameActive && !isShuffling) {
          selectCup(cup);
        }
      });
    });
    
    // Configurar botón de mezclar
    if (shuffleButton) {
      shuffleButton.addEventListener('click', startGame);
    }
    
    // Inicializar juego
    cups.forEach(cup => {
      cup.querySelector('.ball').style.display = 'none';
    });
    console.log('Juego de vasos inicializado');
  });
</script>

<style>
  /* Textura de fondo de mesa de casino */
  .bg-felt-pattern {
    background-image: 
      radial-gradient(circle, rgba(25, 17, 45, 0.6) 1px, transparent 1px),
      radial-gradient(circle, rgba(25, 17, 45, 0.6) 1px, transparent 1px);
    background-size: 20px 20px;
    background-position: 0 0, 10px 10px;
  }

  /* Patrón de puntos para fondo unificado */
  .bg-dots-pattern {
    background-image: 
      radial-gradient(circle, rgba(255, 208, 112, 0.15) 1px, transparent 1px);
    background-size: 30px 30px;
  }
  
  .gradient-text {
    background-image: linear-gradient(45deg, #FFD700, #FFB627);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    display: inline-block;
    position: relative;
  }
  
  .gradient-text::after {
    content: '';
    position: absolute;
    bottom: -3px;
    left: 0;
    width: 100%;
    height: 3px;
    background-image: linear-gradient(45deg, #FFD700, #FFB627);
    border-radius: 2px;
  }
  
  .hero-title {
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
    letter-spacing: 0.05em;
  }
  
  /* Modal de bono mejorado */
  .modal-backdrop {
    transition: opacity 0.5s ease;
  }
  
  .modal-card {
    transform-origin: center;
    transform: scale(0);
    transition: transform 0.7s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    width: 95%;
    max-width: 600px;
  }
  
  .modal-card.scale-100 {
    transform: scale(1);
  }
  
  .modal-card-inner {
    max-height: 90vh;
    overflow-y: auto;
    box-shadow: 0 10px 50px rgba(0, 0, 0, 0.8);
  }
  
  .bonus-icon {
    animation: pulse-glow 2s infinite alternate;
  }
  
  @keyframes pulse-glow {
    0% {
      box-shadow: 0 0 10px rgba(255, 208, 112, 0.5);
    }
    100% {
      box-shadow: 0 0 30px rgba(255, 208, 112, 0.8);
    }
  }
  
  .casino-gold {
    color: #FFD700;
  }
  
  .text-casino-gold {
    color: #FFD700;
  }
  
  /* Botón de acción */
  .cta-button {
    position: relative;
    overflow: hidden;
    box-shadow: 0 0 15px rgba(22, 163, 74, 0.5);
    font-size: 1.2rem;
    padding: 0.8rem;
  }
  
  .cta-button svg {
    width: 1.2rem;
    height: 1.2rem;
  }
  
  .cta-button::after {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: linear-gradient(
      to bottom right,
      rgba(255, 255, 255, 0) 0%,
      rgba(255, 255, 255, 0.2) 50%,
      rgba(255, 255, 255, 0) 100%
    );
    transform: rotate(45deg);
    animation: shine 3s infinite;
  }
  
  /* Animaciones */
  @keyframes shine {
    0% {
      left: -100%;
      top: -100%;
    }
    100% {
      left: 100%;
      top: 100%;
    }
  }
  
  @keyframes pop-in {
    0% {
      transform: scale(0);
    }
    70% {
      transform: scale(1.1);
    }
    100% {
      transform: scale(1);
    }
  }
  
  .animate-pulse-slow {
    animation: pulse-slow 2s infinite;
  }
  
  @keyframes pulse-slow {
    0%, 100% {
      opacity: 0.8;
    }
    50% {
      opacity: 1;
    }
  }
  
  .animate-spin-slow {
    animation: spin 8s linear infinite;
  }
  
  @keyframes spin {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }
  
  .animate-pop-in {
    animation: pop-in 0.5s forwards;
  }
  
  /* Botón de cierre del modal */
  #close-modal {
    transition: transform 0.2s ease;
  }
  
  #close-modal:hover {
    transform: scale(1.1);
  }
  
  /* Estilos para el juego de vasos */
  .cups-game-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    position: relative;
    margin: 0 auto;
    max-width: 400px;
  }
  
  #cups-container {
    position: relative;
    perspective: 800px;
  }
  
  .cup-position {
    width: 80px;
    position: relative;
    height: 150px;
    display: flex;
    justify-content: center;
    align-items: flex-end;
  }
  
  /* Estilos para el vaso rojo tipo Solo cup */
  .cup {
    width: 80px;
    height: 120px;
    cursor: pointer;
    position: relative;
    transform-style: preserve-3d;
    transform-origin: center bottom;
    transition: transform 0.4s ease-out;
    background: linear-gradient(to right, #c41e1e, #fa0000, #c41e1e);
    border-radius: 0 0 8px 8px;
    box-shadow: 3px 3px 8px rgba(0,0,0,0.3);
    display: flex;
    justify-content: center;
    align-items: flex-end;
    overflow: visible;
    z-index: 10;
    /* Forma cónica específica para vasos rojo Solo */
    clip-path: polygon(0% 92%, 100% 92%, 90% 0%, 10% 0%);
  }
  
  /* Líneas decorativas del vaso rojo */
  .cup::before {
    content: '';
    position: absolute;
    top: 20px;
    left: 0;
    width: 100%;
    height: 6px;
    background: rgba(255, 255, 255, 0.05);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    border-bottom: 1px solid rgba(180, 0, 0, 0.3);
  }
  
  .cup::after {
    content: '';
    position: absolute;
    top: 40px;
    left: 0;
    width: 100%;
    height: 6px;
    background: rgba(255, 255, 255, 0.05);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    border-bottom: 1px solid rgba(180, 0, 0, 0.3);
  }
  
  .cup:hover {
    transform: translateY(-10px);
  }
  
  /* Animación para mostrar la pelota - mejorada */
  .cup.reveal {
    transform: translateY(-80px);
    transition: transform 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }
  
  /* Animación para destacar el vaso con la pelota */
  @keyframes highlight-cup {
    0%, 100% { box-shadow: 0 0 0 rgba(255, 215, 0, 0); }
    50% { box-shadow: 0 0 20px rgba(255, 215, 0, 0.8); }
  }
  
  .ball {
    width: 30px;
    height: 30px;
    background: radial-gradient(circle at 30% 30%, white, #e6e6e6);
    border-radius: 50%;
    box-shadow: 0 4px 8px rgba(0,0,0,0.3), inset -2px -2px 5px rgba(0,0,0,0.1);
    position: absolute;
    bottom: 15px;
    display: none;
    z-index: 5;
    transition: opacity 0.3s ease, transform 0.3s ease;
  }
  
  .cup.lifted {
    transform: translateY(-70px) rotateX(30deg);
    cursor: default;
  }
  
  .cup.shake {
    animation: shake 0.5s ease-in-out;
  }
  
  @keyframes shake {
    0%, 100% { transform: translateY(-70px) rotateX(30deg) translateX(0); }
    20%, 60% { transform: translateY(-70px) rotateX(30deg) translateX(-5px); }
    40%, 80% { transform: translateY(-70px) rotateX(30deg) translateX(5px); }
  }
  
  .cup.winner {
    animation: winner 1s ease-in-out;
  }
  
  @keyframes winner {
    0%, 100% { transform: translateY(-70px) rotateX(30deg) scale(1); }
    50% { transform: translateY(-70px) rotateX(30deg) scale(1.1); }
  }
  
  #shuffle-cups-btn {
    margin-top: 20px;
    transition: all 0.3s ease;
  }
  
  #shuffle-cups-btn:hover .shuffle-icon {
    animation: rotate 1s infinite linear;
  }
  
  @keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
  
  #game-instruction {
    height: 30px;
    transition: all 0.3s ease;
  }
  
  /* Responsive */
  @media (max-width: 768px) {
    .cup-position {
      width: 70px;
    }
    
    .cup {
      width: 70px;
      height: 100px;
    }
    
    .ball {
      width: 25px;
      height: 25px;
    }
    
    .modal-card-inner {
      width: 90%;
      max-width: 500px;
      margin: 0 auto;
    }
    
    .modal-content {
      padding: 1.2rem;
    }
    
    .bonus-icon {
      width: 5rem;
      height: 5rem;
    }
    
    .cta-button {
      padding: 0.6rem;
      font-size: 1rem;
    }
    
    .cta-button svg {
      width: 1rem;
      height: 1rem;
    }
  }
  
  @media (max-width: 480px) {
    .cup-position {
      width: 60px;
    }
    
    .cup {
      width: 60px;
      height: 90px;
    }
    
    .ball {
      width: 20px;
      height: 20px;
      bottom: 10px;
    }
    
    .modal-content {
      padding: 1rem;
    }
    
    .bonus-icon {
      width: 4rem;
      height: 4rem;
    }
    
    #modal-title {
      font-size: 1.5rem;
    }
    
    #modal-subtitle {
      font-size: 1.2rem;
    }
    
    #modal-description {
      font-size: 0.9rem;
    }
    
    .feature-text {
      font-size: 0.8rem;
    }
    
    .cta-button {
      padding: 0.5rem;
      font-size: 0.9rem;
    }
    
    .cta-button svg {
      width: 0.8rem;
      height: 0.8rem;
    }
  }
  
  /* Animaciones para elementos flotantes */
  .floating-element:nth-child(1) {
    animation: floatElement1 6s ease-in-out infinite;
  }
  
  .floating-element:nth-child(2) {
    animation: floatElement2 9s ease-in-out infinite;
  }
  
  .floating-element:nth-child(3) {
    animation: floatElement3 8s ease-in-out infinite;
  }
  
  .floating-element:nth-child(4) {
    animation: floatElement4 10s ease-in-out infinite;
  }
  
  @keyframes floatElement1 {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    33% { transform: translateY(-15px) rotate(5deg); }
    66% { transform: translateY(5px) rotate(-5deg); }
  }
  
  @keyframes floatElement2 {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(-8deg); }
  }
  
  @keyframes floatElement3 {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    25% { transform: translateY(-10px) rotate(8deg); }
    75% { transform: translateY(-18px) rotate(-5deg); }
  }
  
  @keyframes floatElement4 {
    0%, 100% { transform: translateY(0) rotate(0deg) scale(1); }
    30% { transform: translateY(-15px) rotate(10deg) scale(1.1); }
    60% { transform: translateY(8px) rotate(-5deg) scale(0.95); }
  }

  /* Efectos de luz fluida (rojo y azul) */
  .fluid-light {
    position: absolute;
    border-radius: 100%;
    filter: blur(60px);
    opacity: 0.7;
    mix-blend-mode: screen;
  }
  
  .fluid-red {
    background: radial-gradient(circle, rgba(255, 50, 50, 1) 0%, rgba(180, 0, 0, 0.8) 60%, rgba(100, 0, 0, 0) 100%);
    width: 60vw;
    height: 60vw;
    max-width: 700px;
    max-height: 700px;
    top: 5%;
    left: 25%;
    animation: fluidMove1 5s ease-in-out infinite alternate;
  }
  
  .fluid-blue {
    background: radial-gradient(circle, rgba(50, 100, 255, 1) 0%, rgba(0, 40, 180, 0.8) 60%, rgba(0, 0, 100, 0) 100%);
    width: 55vw;
    height: 55vw;
    max-width: 650px;
    max-height: 650px;
    bottom: 10%;
    right: 10%;
    animation: fluidMove2 7s ease-in-out infinite alternate;
  }
  
  .fluid-red-small {
    background: radial-gradient(circle, rgba(255, 50, 50, 1) 0%, rgba(180, 0, 0, 0.8) 60%, rgba(100, 0, 0, 0) 100%);
    width: 35vw;
    height: 35vw;
    max-width: 400px;
    max-height: 400px;
    bottom: 15%;
    left: 10%;
    animation: fluidMove3 6s ease-in-out infinite alternate;
  }
  
  .fluid-blue-small {
    background: radial-gradient(circle, rgba(50, 100, 255, 1) 0%, rgba(0, 40, 180, 0.8) 60%, rgba(0, 0, 100, 0) 100%);
    width: 30vw;
    height: 30vw;
    max-width: 350px;
    max-height: 350px;
    top: 20%;
    right: 5%;
    animation: fluidMove4 8s ease-in-out infinite alternate;
  }
  
  @keyframes fluidMove1 {
    0% { transform: translate(0, 0) rotate(0deg); }
    25% { transform: translate(15%, -15%) rotate(90deg); }
    50% { transform: translate(0, -15%) rotate(180deg); }
    75% { transform: translate(-15%, 0) rotate(270deg); }
    100% { transform: translate(0, 15%) rotate(360deg); }
  }
  
  @keyframes fluidMove2 {
    0% { transform: translate(0, 0) rotate(0deg); }
    25% { transform: translate(-15%, 0) rotate(-90deg); }
    50% { transform: translate(0, 15%) rotate(-180deg); }
    75% { transform: translate(15%, 0) rotate(-270deg); }
    100% { transform: translate(0, -15%) rotate(-360deg); }
  }
  
  @keyframes fluidMove3 {
    0% { transform: translate(0, 0) rotate(0deg); }
    25% { transform: translate(15%, 15%) rotate(90deg); }
    50% { transform: translate(15%, 0) rotate(180deg); }
    75% { transform: translate(0, -15%) rotate(270deg); }
    100% { transform: translate(-15%, 0) rotate(360deg); }
  }
  
  @keyframes fluidMove4 {
    0% { transform: translate(0, 0) rotate(0deg); }
    25% { transform: translate(0, 15%) rotate(-90deg); }
    50% { transform: translate(-15%, 0) rotate(-180deg); }
    75% { transform: translate(0, -15%) rotate(-270deg); }
    100% { transform: translate(15%, 0) rotate(-360deg); }
  }
</style>